一、功能需求分析
1.1 功能性需求
功能模块	需求描述	优先级
图像上传	用户通过前端界面上传待检测的产品图像（支持常见格式如jpg、png）	高
缺陷检测	系统调用深度学习模型对图像进行推理，输出缺陷类别、位置框坐标及置信度分数	高
结果可视化	在检测结果图像上绘制缺陷标注框（含类别标签与置信度），并展示在界面中	高
历史记录管理	存储每次检测的原始图像、结果图像、缺陷统计数据及检测时间，支持列表查看与详情检索	中
统计看板	以图表形式展示缺陷类型分布、历史检测趋势等统计数据	中
检测报告生成	根据单次检测结果生成结构化报告（含产品信息、缺陷明细、判定结论等）	中
1.2 非功能性需求
响应性能：单张图像检测推理时间 ≤ 3秒（在GPU环境下）

系统架构：B/S架构，用户通过浏览器访问，无需安装客户端

可扩展性：算法模块与业务逻辑解耦，便于后续替换或升级检测模型

二、方案论证
2.1 技术选型比较
选型维度	方案A	方案B	选择理由
后端框架	Flask	FastAPI	FastAPI原生支持异步、自动生成API文档，更契合现代Web服务开发需求
检测模型	Faster R-CNN	YOLOv8	YOLOv8为单阶段检测器，推理速度更快，适合工业实时检测场景，且生态完善、预训练权重丰富
前端框架	原生HTML+JS	Vue.js	Vue.js组件化开发效率高，响应式数据绑定便于实现动态结果展示
数据库	MySQL	SQLite	系统为单机演示级应用，SQLite轻量级、零配置，符合课程设计规模
2.2 可行性分析
技术可行性：YOLOv8已有成熟的Python接口和预训练权重，OpenCV图像处理库功能完备，前后端分离架构开发路径清晰

数据可行性：工业缺陷检测领域有多个公开数据集（如PCB缺陷数据集、钢材表面缺陷数据集等），可从飞桨AI Studio、ModelScope等平台获取

资源可行性：开发环境可配置GPU加速（NVIDIA CUDA），若无可使用CPU模式运行（推理速度略降）

三、方案规划
3.1 技术栈
层级	技术选型	说明
前端	Vue.js 3 + Axios + ECharts	Vue构建交互界面，Axios处理HTTP请求，ECharts实现统计图表可视化
后端	Python 3.10 + FastAPI + Uvicorn	FastAPI提供RESTful API服务，Uvicorn为ASGI服务器
算法模块	PyTorch + Ultralytics YOLOv8 + OpenCV	YOLOv8模型加载与推理，OpenCV进行图像预处理与后处理绘制
数据库	SQLite + SQLAlchemy ORM	SQLite存储检测记录，SQLAlchemy提供ORM操作接口
版本控制	Git + GitHub	全程git管理，小步提交，保留完整提交历史
3.2 需配置的外部资源
资源类型	具体内容	来源/获取方式
预训练模型权重	YOLOv8s.pt（轻量级版本）	Ultralytics官方GitHub仓库，通过pip install ultralytics自动下载
公开数据集	PCB缺陷数据集（或NEU钢材缺陷数据集）	飞桨AI Studio（https://aistudio.baidu.com/）、ModelScope（https://modelscope.cn）
AI编程工具	Deepseek / Claude Code / 通义灵码（任选其一）	用于vibecoding协作开发，辅助代码生成与调试
3.3 数据集来源
本系统拟采用以下公开工业缺陷数据集之一：

PCB缺陷数据集：包含印刷电路板常见的缺孔、鼠咬、开路、短路等缺陷类型，图像尺寸多样，标注格式为XML

NEU钢材表面缺陷数据集：包含6类钢材表面缺陷（裂纹、夹杂、麻点等），共1800余张图像，已提供标准标注

数据集需进行格式转换（转换为YOLO所需的txt格式）和划分为训练集/验证集（若涉及模型微调则需此步骤；若仅使用预训练权重推理，则只需准备测试图片样本）。

3.4 前端UI方案
布局设计：左右分栏布局。左侧为上传区与检测结果展示区（含原图与标注后对比），右侧为历史记录列表与统计看板

交互流程：用户点击上传按钮选择图像 → 自动触发检测请求 → 检测过程中显示加载动画 → 完成后左侧显示标注结果，右侧历史记录更新，统计图表同步刷新

视觉风格：工业风格色调（深蓝/灰色为主），突出数据可视化效果

3.5 后端算法方案
图像预处理：接收前端上传的图像文件，使用OpenCV统一缩放至模型输入尺寸（如640×640），进行归一化处理

模型推理：加载YOLOv8s预训练权重，执行model.predict()推理，返回检测结果（boxes、class_ids、confidences）

结果后处理：将检测框坐标映射回原始图像尺寸，使用OpenCV在图像上绘制标注框与类别标签，保存结果图像

数据持久化：将原始图像路径、结果图像路径、缺陷统计（各类别数量）、检测时间等写入SQLite数据库

四、技术路线
text
用户上传图像 → 前端(Vue.js) → 后端API(FastAPI) 
    → 算法模块(YOLOv8+OpenCV) → 缺陷检测与标注 
    → 数据库存储(SQLite) → 前端展示标注结果与统计
具体流程：

前端交互层：用户通过浏览器访问系统页面，选择本地图像文件并提交

API网关层：FastAPI接收上传请求，校验文件格式，调用算法服务

算法计算层：YOLOv8模型加载 → 图像预处理 → 模型推理 → 结果解析与绘制

数据存储层：SQLAlchemy ORM将检测记录写入SQLite数据库

结果返回层：后端返回检测结果JSON（含缺陷类别、坐标、置信度）及结果图像URL，前端渲染展示

五、计划安排
阶段	时间	主要任务	产出物
工具配置与学习	D1-D2	配置开发环境（Python、VS Code/Cursor、Git），学习vibecoding方法，完成选题调研	可运行开发环境、选题说明
方案设计	D3-D5	撰写需求规格说明书与方案设计文档，确认技术栈与数据来源	方案设计.md
数据资源整理	D6	下载公开数据集，清洗处理，转换为YOLO格式，入库	清洗后的数据集
系统详细开发	D7-D8	开发后端API、算法模块、前端UI，集成联调，编写自动化测试	可运行Demo、测试用例
集成调试与报告撰写	D9	端到端测试、修复Bug、撰写设计报告、录制演示视频、制作答辩PPT	设计报告、演示视频、PPT
答辩与提交	D10	提交所有交付物，参加现场答辩	完整提交物
六、参考文献
[1] 张智海等.《制造智能技术基础》.北京：清华大学出版社，2022.

[2] Ultralytics YOLOv8 Documentation. https://docs.ultralytics.com/

[3] 飞桨AI Studio. https://aistudio.baidu.com/ 

[4] 魔搭ModelScope. https://modelscope.cn