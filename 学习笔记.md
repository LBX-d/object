学号： 202412020111
姓名： 林柏旭
GitHub仓库地址： https://github.com/LBX-d/object

选题名称： 智能车间产品外观缺陷检测与报告系统

1. AI工具学习与选型调研
1.1 当前主流AI编程辅助工具概览
在本阶段课程设计中，我重点体验并调研了目前主流的AI编程辅助工具，旨在提升全栈开发效率，理解“Vibe Coding”的开发模式。

工具名称	核心优势	适用场景	备注
Cursor	编辑器级AI，支持代码库上下文问答，可读取整个项目文件	复杂项目重构、代码解释、Bug定位	目前体验最佳的AI编辑器之一，本课程设计首选
GitHub Copilot	代码补全准确率高，与VS Code深度集成，支持多语言	日常编码、函数实现、单元测试生成	行业标杆，需付费订阅
通义灵码 (Tongyi Lingma)	阿里云生态整合，对中文注释支持良好，免费	企业级后端开发、微服务架构	免费且功能全面，国产工具备选
文心快码 (Comate)	对中文语境理解友好，支持国产芯片	国内开发环境、企业内部部署	百度出品，对中文注释支持良好
1.2 拟选用AI模型与工具链
经过对比，结合本课程设计（智能车间产品外观缺陷检测与报告系统）的需求，我计划采用以下组合：
主要编程辅助： Cursor（内置Claude 3.5 Sonnet / GPT-4o模型）。
理由： 在处理多文件关联逻辑时表现优异，适合本项目涉及的后端API开发、YOLO模型集成、前后端联调等复杂逻辑。
辅助编程工具： GitHub Copilot。
理由： 作为日常编码习惯，用于快速生成样板代码、HTML模板和重复性的数据处理函数。
大模型选型（非编程辅助，用于NLP报告生成模块）： DeepSeek API（输出检测报告文本）。
理由： DeepSeek在中文文本生成方面表现出色，成本低，适合课程设计场景。如遇网络限制，备选方案为本地模板引擎（Jinja2）生成结构化报告。
编程辅助模型选择： 以 Claude 3.5 Sonnet 为主（逻辑推理和代码生成质量高），DeepSeek-Coder 为辅（特定算法实现和代码优化）。

2. Git 原理与功能深度理解
2.1 Git 核心工作原理
Git 是一个分布式版本控制系统，其核心原理基于内容寻址文件系统。
快照 (Snapshots) 而非差异 (Differences)：不同于SVN只存储文件的差异，Git在每次Commit时都会保存所有文件当时的完整状态（快照）。如果文件未修改，则只保留指向上次文件的链接。
SHA-1 哈希值：每个文件、目录、提交对象都通过SHA-1算法生成40位的哈希字符串作为唯一ID，确保了版本历史的完整性。

三个工作区域：

工作区 (Working Directory)：本地修改文件的地方。
暂存区 (Staging Area / Index)：通过 git add 将修改暂存，准备提交。
本地仓库 (Local Repository)：通过 git commit 将暂存区内容永久保存。
2.2 本课程设计常用 Git 功能清单
命令	功能描述	在本项目中的使用场景
git init	在本地初始化Git仓库	项目开始时创建本地版本库
git remote add origin	关联远程仓库	将本地仓库与Gitee/GitHub远程仓库连接
git branch	查看/创建/删除分支	开发各功能模块时创建独立分支（如 feature/yolo_detection）
git checkout -b	创建并切换到新分支	隔离不同功能的开发环境，避免干扰主分支
git add .	将所有变更添加到暂存区	阶段性保存工作进度（如完成一个API接口后）
git commit -m "..."	提交暂存区内容并添加注释	按照“规范式提交”（如 feat: 添加缺陷检测API）记录变更点
git push origin main	推送本地提交到远程仓库	备份代码，确保代码不丢失
git log --oneline	查看简洁提交历史	回顾开发过程，准备过程档案
git diff	查看文件具体变更内容	在AI修改代码后，人工审查变更点
3. 课程设计选题调研与项目规划
3.1 选题方向
本次课程设计拟定为 “智能车间产品外观缺陷检测与报告系统”。

3.2 选题背景与痛点
背景： 在现代化制造车间中，产品外观质量检测是保障出货品质的关键环节。传统的人工肉眼检测存在效率低、易疲劳、标准不统一等问题。随着计算机视觉和深度学习技术的发展，基于AI的自动光学检测（AOI）已成为工业质检的主流趋势。

痛点： 当前中小型制造企业的质检环节面临以下问题：

人工检测速度慢，难以满足生产线节拍要求。
检测标准因人而异，主观性强，质量一致性差。
缺陷数据难以数字化存档，无法进行质量追溯和统计分析。
缺乏自动化的检测报告生成机制，增加了质检员文书工作负担。
3.3 核心功能模块（Vibe Coding 实现计划）
模块	功能描述	实现技术	对应技术方向
前端界面	图片上传、检测触发、结果展示、报告查看、历史记录	Vue 3 + Element-Plus	前端UI
后端服务	API路由、业务逻辑编排、算法调度	Python + FastAPI	后端服务
数据库	存储检测记录（图片路径、结果、等级、报告、时间戳）	SQLite / MySQL + SQLAlchemy	数据库
CV检测模块	YOLOv8模型加载与推理，识别缺陷类型和位置	PyTorch + Ultralytics YOLOv8	技术方向1：计算机视觉
ML分级模块	根据缺陷检测结果，判定产品质量等级（A/B/C级）	Scikit-learn（决策树/随机森林）	技术方向2：机器学习
NLP报告模块	根据检测结果自动生成中文检测报告文本	DeepSeek API / Jinja2模板引擎	技术方向3：自然语言处理
3.4 系统架构草图（Vibe Coding辅助设计）
text
┌─────────────────────────────────────────────────────────────┐
│                      前端 UI (Vue 3)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────────┐  │
│  │ 图片上传  │  │ 检测触发  │  │ 结果展示/报告查看/历史   │  │
│  └──────────┘  └──────────┘  └──────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP (RESTful API)
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                   后端服务 (FastAPI)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │ /api/detect │  │ /api/grade  │  │  /api/report        │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│                           │                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  算法引擎层                                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │   │
│  │  │ YOLOv8      │  │ 决策树分类器 │  │ NLP报告生成│  │   │
│  │  │ 缺陷检测    │  │ 质量分级    │  │ (API/模板) │  │   │
│  │  └─────────────┘  └─────────────┘  └────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据库 (SQLite)                          │
│          ┌────────────────────────────────────┐             │
│          │ 检测记录表 (id, 图片路径, 缺陷类型, │             │
│          │ 置信度, 等级, 报告文本, 时间戳)    │             │
│          └────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘
3.5 数据来源规划
数据类型	来源	规模	处理方式
缺陷图片	NEU-DET公开数据集（东北大学钢材表面缺陷数据集）	1800张（6类缺陷，每类300张）	按8:1:1划分训练/验证/测试集，转换为YOLO格式标注
质量等级标签	根据缺陷面积、数量等指标自行构建规则生成	对应每张图片生成A/B/C级标签	用于训练分级模型（决策树）
检测记录	系统运行后自动产生	不限	存入SQLite数据库，用于历史查询和分析
3.6 关键技术路径说明
（1）计算机视觉（缺陷检测）—— YOLOv8
YOLOv8是目前工业界广泛使用的目标检测算法，其速度快、精度高，适合部署在质检工位边缘设备上。本系统使用Ultralytics官方提供的预训练权重，在NEU-DET数据集上进行微调（或直接使用预训练模型进行零样本推理），实现对裂纹、划痕、麻面等6类钢材表面缺陷的自动识别与定位。

（2）机器学习（质量分级）—— 决策树
决策树算法具有良好的可解释性，适合工业场景中对决策逻辑有明确要求的应用。本系统根据缺陷检测模块输出的特征（缺陷数量、最大缺陷面积、缺陷总占比等），训练决策树分类器，将产品划分为A级（合格）、B级（需返工）、C级（报废），分级逻辑清晰可追溯。

（3）自然语言处理（报告生成）—— 提示词工程 + 大模型API
利用大语言模型（DeepSeek）的文本生成能力，将检测结果（缺陷类型、置信度、等级）作为输入，通过精心设计的提示词（Prompt）引导模型生成结构化的中文检测报告，包括检测结论、质量评价和处理建议。如API调用受限，则采用Jinja2模板引擎生成标准化报告文本作为备选方案。

4. Vibe Coding 实践规划
4.1 规格驱动开发流程
阶段	任务	输入（给AI的上下文）	输出
阶段1：需求拆解	向AI描述项目背景和功能需求	本笔记第3节的选题背景和功能模块	详细的功能清单和技术选型建议
阶段2：AI制定计划	让AI根据需求生成分步开发计划	需求规格说明书 + 系统架构图	按前后端、算法模块拆分的开发任务列表
阶段3：分步实现	按计划逐个模块让AI生成代码	当前模块的输入输出规格 + 代码风格要求	各模块可运行的代码
阶段4：逐步验收	每完成一个模块进行人工测试	测试用例 + 预期结果	通过测试的模块，Git提交记录
4.2 上下文工程策略
项目规则文件：在项目根目录创建 CODING_RULES.md，明确代码风格、命名规范、API设计规范，让AI在生成代码时遵循统一标准。
“先读后改”策略：在让AI修改已有代码时，先将相关代码文件内容完整复制到对话中，再描述修改需求，避免AI生成与现有逻辑不兼容的代码。
小步提交：每完成一个可独立运行的功能点，立即执行 git add . 和 git commit -m "feat: xxx"，确保变更可追溯、可回滚。

4.3 调试与人工接管策略
策略	具体做法	在本项目中的适用场景
回滚	遇到AI修改导致系统崩溃时，执行 git checkout 回退到上一个稳定版本	YOLO模型集成失败、前后端接口不匹配时
缩小改动	不一次性让AI修改整个文件，而是限制在单个函数或代码块内	优化检测算法、修复特定API的Bug时
最小复现	将复杂问题拆解为最小可复现示例，单独调试	定位模型推理报错、数据库连接异常时
5. 学习心得与下一阶段计划
5.1 AI工具体会
通过本阶段学习，我发现AI编码工具的核心价值不在于“替代编码”，而在于降低试错成本和加速原型验证（Vibe Coding）。
具体体会：
需求拆解能力 > 编码能力：AI能够快速生成代码，但前提是开发者能够将业务需求清晰、结构化地描述给AI。好的Prompt = 好的代码。
Code Review能力变得更重要：AI生成的代码可能存在安全隐患、性能问题或逻辑错误，开发者必须具备审查和修正的能力。
上下文管理是Vibe Coding的核心：让AI充分理解项目背景和现有代码，比反复修改多轮更高效。项目规则文件和“先读后改”策略是提升AI生成质量的关键。

5.2 Git 使用反思
良好的 Commit 习惯（约定式提交）和分支管理策略是团队协作的基石。在本项目中，我将遵循以下提交规范：
feat: 添加XXX功能（新功能）
fix: 修复XXX问题（Bug修复）
docs: 更新XXX文档（文档变更）
refactor: 重构XXX模块（代码重构）

5.3 下一步任务
配置开发环境，初始化项目前后端结构。
下载并整理NEU-DET数据集，完成数据预处理。
编写YOLOv8缺陷检测的测试用例，验证模型推理可用。

创建FastAPI后端骨架，编写第一个API接口（/api/health）。
设计SQLite数据库表结构（检测记录表）。